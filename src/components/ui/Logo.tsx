
import React from 'react';
import { BookOpen } from 'lucide-react';

const Logo = () => {
  return (
    <div className="flex items-center justify-center w-8 h-8 rounded-lg bg-studynest-purple text-white">
      <BookOpen size={18} />
    </div>
  );
};

export default Logo;
